i

class Mesh:
    self.U
    self.V
    self.P
    self.UVCoeffs
    self.PCoeffs

    def discretize(self, FLAG):
        DO STUFF

        if FLAG in "U":
            self.UVCoeffs = A
            CO = self.UVCoeffs
        elif FLAG in "V":
            VAR = self.V
            CO = self.UVCoeffs
        elif FLAG in "P":
            VAR = self.V
            CO = self.PCoeffs


        kfksdj
        return COEFFS





mesh = Mesh()

mesh.discretize("U")
mesh.discretize("V")

DATASTORAGE.AP = mesh.discretize("P")
